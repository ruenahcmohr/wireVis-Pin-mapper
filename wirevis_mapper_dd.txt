Could you please design me a 1-file html/javascript webpage that: a) allows the user to upload an image that is automatically scaled to fit the screen space b) graphically define and add (add button) connectors of 1-wide adjustable pin counts (via spinner) which appear as small labeled (as per dynamic list of text boxes for user input of labels) icon strips over the image, where icons can be edited (double click) rotated and moved by clicking and dragging (left button move, right button rotate) and contain the connector name, and pin labels. pin labels should be divided by rectangles with the first rectangle having a bold line weight. c) contains an interactive text object that contains a dynamically generated wireVis definition for the connectors that have been added, along with information contained in a notes section for the position and rotation of each connector that has been added. (using co-ordinates that pertain to the native resolution of the image and not the scaled view) d) allows, via a button ("export image"), to export the image with the icon overlays, which have teh scale adjusted to match the view on the webpage. e) allow, via a button ("export wireVis"), to download the wireVis information as a text file. f) this tool shall only generate the 'connectors' section of the wireVis file. The 'connectors' section shall only be populated with 'pinlabels' and 'notes' subsection. where by the notes contain the position and rotation information, and 'pinlabels' is constructed as per the design of each added connector. c) has a UI with a chudcore theme.
